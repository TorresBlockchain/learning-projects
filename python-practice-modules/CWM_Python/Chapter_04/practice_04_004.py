# Lesson on keyword arguments

# def increment(number, by):
#    return number + by
#
#
#result = increment(2, 1)
# print(result)

# the following code helps consolidating lines 7 & 8

def increment(number, by):
    return number + by


print(increment(2, 1))

# an example of a keyword argument, would be on line 16 where we can type within the increment (increment(2, by=1))
