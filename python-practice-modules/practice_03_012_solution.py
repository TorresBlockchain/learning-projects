count = 0
for number in range(1, 10):
    if number % 2 == 0:
        count += 1
        print(number)
print(f"We have {count} even numbers")

# Lesson learned
# do not attempt to skip skips while performing a program
# only use abbrev. methods of writing code, but not to derive the answers due to changes of parameters
